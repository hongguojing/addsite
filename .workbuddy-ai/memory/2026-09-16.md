# 2026-09-16

## 文章页右侧目录固定 + 滚动高亮
- 修改 `src/styles/global.css`：给 `.reading-sidecar`（≥1180px 媒体查询）增加 `align-self: start`（grid sticky 必需）、`max-height: calc(100vh - 7rem)` + `overflow-y: auto` + 自定义滚动条样式
- 改进 TOC active 样式：增加左侧紫色竖线指示器 + transition 动画 + 加粗
- 修改 `src/pages/posts/[slug].astro`：用 scroll + rAF 替换 IntersectionObserver 实现滚动 spy，更可靠地追踪当前阅读位置（标题过顶部 28% 触发线即激活，接近底部激活最后一项）
- 构建验证通过
