# 2026-09-16

## 文章页右侧目录固定 + 滚动高亮
- 修改 `src/styles/global.css`：给 `.reading-sidecar`（≥1180px 媒体查询）增加 `align-self: start`（grid sticky 必需）、`max-height: calc(100vh - 7rem)` + `overflow-y: auto` + 自定义滚动条样式
- 改进 TOC active 样式：增加左侧紫色竖线指示器 + transition 动画 + 加粗
- 修改 `src/pages/posts/[slug].astro`：用 scroll + rAF 替换 IntersectionObserver 实现滚动 spy，更可靠地追踪当前阅读位置（标题过顶部 28% 触发线即激活，接近底部激活最后一项）
- 构建验证通过

## 删除侧边栏 quote 文字
- `src/layouts/BaseLayout.astro` 侧边栏原有 `<p class="side-quote">这里不是告诉你"如何变优秀"的地方，而是帮助你理解自己。</p>`，在 commit 8814b1e 中已移除 HTML 元素
- 清理残留：删除 `.side-quote` CSS 类（global.css 主区 + 媒体查询）、清理 BaseLayout 空白行、清理 `.astro/cache`
- 重新构建验证：dist 中无"变优秀""这里不是告诉""side-quote"任何残留

## 文章目录条理性修复
- 8篇文章 H1→H2 转换（共48处）：自我预诊(13)、从社交中疗愈自己(9)、diagnosis-guide(11)、common-myths(5)、从神经科学视角(7)、belike-world-slows(1)、time-management(1)、运动治疗(1)
- 消除所有标题跳级问题（H1→H3 跳级，共10处）
- communication 文章：25个无意义 H3（"❌""✅""不推荐"等）转为粗体文本，目录从56条降至31条
- 从神经科学视角文章：7个 H2 转为 H3（案例和子概念归入上级标题），15平铺→8H2+7H3有层级
- 修复后构建通过

## 主题切换功能（深色/浅色）
- 背景图：水墨山水风格（山、月、江、竹），优化后 238KB 存到 `public/images/bg-light.jpg`
- `src/layouts/BaseLayout.astro`：右上角 ☼/◐ 从装饰 span 改为可点击 `<button class="theme-toggle" data-theme-target="...">`，带 is-active 高亮；末尾添加切换脚本（localStorage 持久化 key=adhd-kb-theme，初始值降级为 prefers-color-scheme）
- `src/styles/global.css`：新增 `.theme-toggle` 按钮样式 + 完整 `[data-theme="light"]` 浅色主题覆写（~60条规则覆盖侧边栏、工具栏、卡片、文章正文、TOC、代码块等所有组件）
- 浅色配色：背景图固定 + 卡片用 rgba(255,255,255,0.72~0.86) 半透明 + backdrop-filter 模糊；文字 #1f2937 / #14213d；强调色 墨绿 #2d8074 + 雾蓝 #5a9ea5
