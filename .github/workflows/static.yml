# ⚠️ 此文件与 deploy.yml 重复（2026-09-13 注释停用，原内容如下，恢复时去掉每行行首的 "# " 即可）
# name: Deploy to GitHub Pages
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6
#
#       - name: Build Astro site
#         uses: withastro/action@v6
#
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v5
